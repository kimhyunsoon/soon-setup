## 응답 규칙

- 항상 한국어로 답변한다.
- **질문 금지**
- **코드 수정으로만 답변한다.**
- **빌드 및 린트 명령어를 실행하지 않는다.**

## 코딩 스타일

- 간결하고 실용적인 코드를 작성
- `any` 타입을 지양한다.
- 모든 함수·메서드에 반환 타입을 명시한다.
- 주석 규칙:
  - 단순한 경우 → 선언부 위 `//` 인라인 주석.
  - 인자 설명이 필요한 경우 → `/** */` JSDoc 형식.
  - `/* */` 블록 주석은 사용하지 않는다.
